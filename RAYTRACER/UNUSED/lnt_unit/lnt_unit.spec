This unit is called the Leaf Node Tester unit (LNT)
It's basic operation is to send off and manage an intersection test with all of the
leaf node's triangles. It contains a mailbox that holds ray intersections that are not in the current Tmax, Tmin


function leaf_node_tester(node,Tmax, Tmin)

  if(Radiance Ray)
    foreach triangle in leaf node (can be done in paralle)
      







  else //Shadow Ray
